flask==3.0.0
flask-socketio==5.3.5
flask-talisman==1.1.0  # Enterprise security: CSP headers, HSTS, security hardening
python-socketio==5.10.0
opencv-python>=4.8.0,<4.10.0  # Compatible with numpy<2
mediapipe==0.10.31; platform_machine == 'x86_64' or platform_machine == 'AMD64'  # Story 8.2 - Windows/x64 platforms
mediapipe==0.10.18; platform_machine == 'aarch64'  # Story 8.2 - ARM64 (Pi) platforms
numpy<2  # Required by MediaPipe
sdnotify>=0.3.2
schedule>=1.2.0  # Story 4.6 - Daily task scheduler for end-of-day summaries
systemd-python>=235
# NOTE: MediaPipe Tasks API migration (Story 8.2)
# - Both 0.10.18 (ARM64) and 0.10.31 (x64) include Tasks API with PoseLandmarker
# - Platform-specific versions ensure compatibility across Windows and Pi
# - Tasks API replaces deprecated Solutions API for future-proofing

# Windows Desktop Client (Story 7.1)
pystray>=0.19.4; sys_platform == 'win32'
Pillow>=10.0.0; sys_platform == 'win32'
winotify>=1.1.0; sys_platform == 'win32'  # Modern replacement for win10toast
pywin32>=306; sys_platform == 'win32'  # Windows API (mutex, MessageBox)
requests>=2.31.0  # HTTP requests for stats API (cross-platform)

# Development & Testing (Story 8.1)
pytest>=9.0.0  # Unit testing framework
pytest-cov>=7.0.0  # Test coverage measurement
pytest-flask>=1.3.0  # Flask testing utilities
