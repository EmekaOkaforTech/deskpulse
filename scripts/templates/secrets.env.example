# DeskPulse Secrets Environment File
# ===================================
# This file contains sensitive configuration that should NOT be in config.ini
#
# Installation:
#   1. Copy this file to /etc/deskpulse/secrets.env
#   2. Generate a secure secret key (see below)
#   3. Set permissions: chmod 600 /etc/deskpulse/secrets.env
#
# Generate a secure key with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
#
# Or use the setup script:
#   ./scripts/setup_config.sh

# Flask secret key for session management
# REQUIRED for production - replace this value!
DESKPULSE_SECRET_KEY=REPLACE_WITH_SECURE_RANDOM_KEY
