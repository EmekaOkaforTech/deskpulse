# DeskPulse Windows Client Dependencies
# Install with: pip install -r requirements-windows.txt

# System Tray Integration
pystray>=0.19.4

# Toast Notifications (Windows 10/11)
winotify>=1.1.0

# WebSocket Connection to Pi Backend (Epic 7 only - REMOVED for Epic 8 standalone)
# Epic 8 uses local IPC (callbacks + event queue) instead of network SocketIO
# python-socketio>=5.12.0  # REMOVED Story 8.4
# python-engineio>=4.8.0   # REMOVED Story 8.4

# HTTP Client for REST API
requests>=2.31.0

# Windows API
pywin32>=306

# Image Handling (for tray icons)
Pillow>=10.0.0

# Camera Enumeration (OPTIONAL - provides enhanced camera names)
# Released January 2, 2026 - enhances camera detection with friendly names
# Application WORKS WITHOUT this package (fallback to basic "Camera 0, Camera 1" names)
# COMMENTED OUT: Install manually if you want enhanced camera names
# Uncomment and run: pip install cv2-enumerate-cameras>=1.1.0
# cv2-enumerate-cameras>=1.1.0  # OPTIONAL - commented out for maximum compatibility
